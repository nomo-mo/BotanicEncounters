<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.FavoriteMapper">

    <resultMap id="favoriteMap" type="com.example.app.domain.Favorite" autoMapping="true">
        <result property="botanicName" column="botanic_name" />
        <result property="imagePath" column="image_path" />
    </resultMap>

    <!-- お気に入り登録 -->
    <insert id="insertFavorite" parameterType="com.example.app.domain.Favorite">
        INSERT INTO favorites (user_id, botanic_name, image_path)
        VALUES (#{userId}, #{botanicName}, #{imagePath})
    </insert>

    <!-- お気に入り削除 -->
    <delete id="deleteFavorite">
        DELETE FROM favorites WHERE user_id = #{userId} AND botanic_name = #{botanicName}
    </delete>

    <!-- ユーザーのお気に入り一覧を取得 -->
    <select id="selectFavoritesByUserId" resultMap="favoriteMap">
        SELECT botanic_name, image_path 
        FROM favorites
        WHERE user_id = #{userId}
    </select>

    <!-- すでにお気に入りかどうかをチェック -->
    <select id="isFavorite" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM favorites WHERE user_id = #{userId} AND botanic_name = #{botanicName}
        )
    </select>

</mapper>



